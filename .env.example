# The Well Configuration

# Server settings
PORT=3000
HOST=localhost

# Persistence settings
PITS_DIR=./pits
PIT_TTL_MINUTES=30
CLEANUP_INTERVAL_MINUTES=5

# Limits (for future use)
MAX_PIT_SIZE_BYTES=5242880
MAX_CONNECTIONS_PER_IP=10
MAX_UPDATE_BYTES_PER_MIN=204800
